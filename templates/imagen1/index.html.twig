{% extends 'base.html.twig' %}

{% block title %}Home
{% endblock %}

{% block principal %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<table border="1">
			<th>Nombre</th>
			<th>Descripción</th>
			<th>N.Likes</th>
			<th>N.Visualizaciones</th>
			<th>N.Descargas</th>
			{% for imagen in imagenes %}
				<tr>
					<td>{{ imagen.getNombre() }}</td>
					<td>{{ imagen.getDescripcion() }}</td>
					<td>{{ imagen.getNumLikes() }}</td>
					<td>{{ imagen.getNumVisualizaciones() }}</td>
					<td>{{ imagen.getNumDescargas() }}</td>
					<td>
						<a href="{{ path('sym_imagen_show', { id: imagen.getId() }) }}" data-toggle="tooltip" data-original title="More information">{{ imagen.getNombre() }}</a>
					</td>

				</tr>
			{% endfor %}
		</table>
	</div>
{% endblock %}
